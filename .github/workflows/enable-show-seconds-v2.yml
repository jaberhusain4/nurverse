name: Activate Show Seconds everywhere

on:
  push:
    paths:
      - '.github/workflows/enable-show-seconds-v2.yml'

jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/prayer-completion-tracker
          fetch-depth: 1
      - name: Patch prayer countdown display
        shell: python
        run: |
          from pathlib import Path
          p = Path('lib/screens/localized_prayer_screen.dart')
          text = p.read_text(encoding='utf-8')
          old = "                    c.timeRemainingForNextPrayer,"
          new = "                    settings.showSeconds ? c.timeRemainingForNextPrayer : _withoutSeconds(c.timeRemainingForNextPrayer),"
          if old not in text:
              raise SystemExit('Expected countdown display not found.')
          text = text.replace(old, new, 1)
          marker = "  Widget _importantTimes(BuildContext context, PrayerController c) {"
          helper = "  String _withoutSeconds(String value) {\n    final match = RegExp(r'^(\\d{2}):(\\d{2})').firstMatch(value.trim());\n    return match == null ? value : '${match.group(1)}:${match.group(2)}';\n  }\n\n"
          if helper.strip() not in text:
              if marker not in text:
                  raise SystemExit('Prayer screen insertion marker not found.')
              text = text.replace(marker, helper + marker, 1)
          p.write_text(text, encoding='utf-8')
      - name: Commit patch and remove workflow
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/screens/localized_prayer_screen.dart .github/workflows/enable-show-seconds-v2.yml
          git commit -m 'fix(settings): hide prayer countdown seconds when disabled'
          git push origin HEAD:fix/prayer-completion-tracker
          git rm .github/workflows/enable-show-seconds-v2.yml
          git commit -m 'chore: remove one-time Show Seconds workflow v2'
          git push origin HEAD:fix/prayer-completion-tracker
