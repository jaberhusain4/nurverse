name: Unify NurVerse Settings Screen

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  unify:
    if: ${{ !contains(github.event.head_commit.message, 'Unify Settings Screen') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Merge Jamaat controls into SettingsScreen
        run: |
          python3 <<'PY'
          from pathlib import Path
          path = Path('lib/screens/settings/settings_screen.dart')
          s = path.read_text(encoding='utf-8')
          s = s.replace("import '../prayer/jamaat_settings_screen.dart';\n", "import '../../services/jamaat_service.dart';\n")
          old = '''          _buildSettingsCard(
            context,
            children: [
              _buildSettingTile(
                context,
                icon: Icons.tune_rounded,
                title: 'জামাআত মোড ও সময়',
                subtitle: 'Automatic / Manual এবং প্রতিটি ওয়াক্তের সময়',
                onTap: () {
                  Navigator.of(context).push(
                    MaterialPageRoute<void>(
                      builder: (_) => const JamaatSettingsScreen(),
                    ),
                  );
                },
              ),
            ],
          ),'''
          new = '''          _buildSettingsCard(
            context,
            children: [
              _buildJamaatSummaryTile(context),
              _buildDivider(context),
              _buildJamaatTimeRow(context, prayer: 'Fajr', title: 'ফজর', icon: Icons.nights_stay_outlined),
              _buildDivider(context),
              _buildJamaatTimeRow(context, prayer: 'Dhuhr', title: 'যোহর / জুমু\\'আ', icon: Icons.wb_sunny_outlined),
              _buildDivider(context),
              _buildJamaatTimeRow(context, prayer: 'Asr', title: 'আসর', icon: Icons.wb_twilight_outlined),
              _buildDivider(context),
              _buildJamaatTimeRow(context, prayer: 'Maghrib', title: 'মাগরিব', icon: Icons.wb_twilight_rounded),
              _buildDivider(context),
              _buildJamaatTimeRow(context, prayer: 'Isha', title: 'ইশা', icon: Icons.nights_stay_rounded),
            ],
          ),'''
          if old not in s:
              raise SystemExit('Expected Jamaat navigation block not found.')
          s = s.replace(old, new, 1)
          marker = "  Widget _buildSectionLabel(BuildContext context, String title, IconData icon) {"
          helpers = r'''  Widget _buildJamaatSummaryTile(BuildContext context) {
    final settings = context.watch<SettingsProvider>();
    return _buildSettingTile(
      context,
      icon: Icons.groups_rounded,
      title: settings.isEnglish ? 'Jamaat Times' : 'জামাআত সময়',
      subtitle: settings.isEnglish
          ? 'Default Dhaka reference or your local mosque time'
          : 'ঢাকার ডিফল্ট সময় অথবা আপনার এলাকার মসজিদের সময়',
    );
  }

  Widget _buildJamaatTimeRow(BuildContext context, {required String prayer, required String title, required IconData icon}) {
    final settings = context.watch<SettingsProvider>();
    final isDefault = JamaatService.isDhakaDefault(prayer);
    return _buildSettingTile(
      context,
      icon: icon,
      title: title,
      subtitle: isDefault
          ? (settings.isEnglish ? 'Dhaka default time' : 'ঢাকার ডিফল্ট সময়')
          : (settings.isEnglish ? 'Local mosque time' : 'আপনার এলাকার মসজিদের সময়'),
      trailing: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          Text(
            _formatJamaatDisplay(JamaatService.get(prayer), settings.is24Hour),
            style: Theme.of(context).textTheme.titleSmall?.copyWith(
              color: Theme.of(context).colorScheme.primary,
              fontWeight: FontWeight.w800,
            ),
          ),
          IconButton(
            tooltip: settings.isEnglish ? 'Set time' : 'সময় সেট করুন',
            icon: const Icon(Icons.edit_outlined, size: 19),
            onPressed: () => _showJamaatTimeDialog(context, prayer, title),
          ),
        ],
      ),
    );
  }

  String _formatJamaatDisplay(String value, bool is24Hour) {
    final match = RegExp(r'^(\d{1,2}):(\d{2})\s*(AM|PM)$', caseSensitive: false).firstMatch(value.trim());
    if (match == null) return value.trim().isEmpty ? '--:--' : value.trim();
    var hour = int.tryParse(match.group(1)!) ?? 0;
    final minute = match.group(2)!;
    final period = match.group(3)!.toUpperCase();
    if (!is24Hour) return '$hour:$minute $period';
    if (period == 'PM' && hour != 12) hour += 12;
    if (period == 'AM' && hour == 12) hour = 0;
    return '${hour.toString().padLeft(2, '0')}:$minute';
  }

  Future<void> _showJamaatTimeDialog(BuildContext context, String prayer, String title) async {
    final settings = context.read<SettingsProvider>();
    await JamaatService.initialize();
    final controller = TextEditingController(text: JamaatService.get(prayer));
    String? error;
    await showDialog<void>(
      context: context,
      builder: (dialogContext) => StatefulBuilder(
        builder: (dialogContext, setDialogState) => AlertDialog(
          title: Text('$title জামাআতের সময়'),
          content: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              TextField(
                controller: controller,
                keyboardType: TextInputType.text,
                decoration: InputDecoration(
                  labelText: 'সময়',
                  hintText: settings.is24Hour ? 'যেমন 17:00' : 'যেমন 5:00 PM',
                  errorText: error,
                ),
              ),
              const SizedBox(height: 8),
              if (!JamaatService.isDhakaDefault(prayer))
                TextButton.icon(
                  onPressed: () async {
                    await JamaatService.useDefault(prayer);
                    if (dialogContext.mounted) Navigator.of(dialogContext).pop();
                  },
                  icon: const Icon(Icons.public_rounded, size: 18),
                  label: const Text('ঢাকার ডিফল্ট সময় ব্যবহার করুন'),
                ),
            ],
          ),
          actions: [
            TextButton(onPressed: () => Navigator.of(dialogContext).pop(), child: const Text('বাতিল')),
            FilledButton(
              onPressed: () async {
                final ok = await _saveJamaatInput(settings, prayer, controller.text);
                if (ok && dialogContext.mounted) {
                  Navigator.of(dialogContext).pop();
                } else {
                  setDialogState(() => error = 'সঠিক সময় লিখুন, যেমন 5:00 PM বা 17:00');
                }
              },
              child: const Text('সংরক্ষণ'),
            ),
          ],
        ),
      ),
    );
    controller.dispose();
  }

  Future<bool> _saveJamaatInput(SettingsProvider settings, String prayer, String input) async {
    final text = input.trim();
    final amPm = RegExp(r'^(\d{1,2}):(\d{2})\s*(AM|PM)$', caseSensitive: false).firstMatch(text);
    if (amPm != null) {
      final hour = int.tryParse(amPm.group(1)!) ?? -1;
      final minute = int.tryParse(amPm.group(2)!) ?? -1;
      if (hour < 1 || hour > 12 || minute < 0 || minute > 59) return false;
      return settings.setJamaatTime(prayer, '$hour:${minute.toString().padLeft(2, '0')} ${amPm.group(3)!.toUpperCase()}');
    }
    final twentyFour = RegExp(r'^(\d{1,2}):(\d{2})$').firstMatch(text);
    if (twentyFour != null) {
      final hour = int.tryParse(twentyFour.group(1)!) ?? -1;
      final minute = int.tryParse(twentyFour.group(2)!) ?? -1;
      if (hour < 0 || hour > 23 || minute < 0 || minute > 59) return false;
      final period = hour >= 12 ? 'PM' : 'AM';
      final displayHour = hour % 12 == 0 ? 12 : hour % 12;
      return settings.setJamaatTime(prayer, '$displayHour:${minute.toString().padLeft(2, '0')} $period');
    }
    return false;
  }

'''
          if marker not in s:
              raise SystemExit('SettingsScreen helper marker not found.')
          s = s.replace(marker, helpers + marker, 1)
          path.write_text(s, encoding='utf-8')
          PY
      - name: Verify and commit
        run: |
          grep -q "_buildJamaatTimeRow" lib/screens/settings/settings_screen.dart
          grep -q "title: 'সময় ফরম্যাট'" lib/screens/settings/settings_screen.dart
          ! grep -q "JamaatSettingsScreen" lib/screens/settings/settings_screen.dart
          git diff --check
          git config user.name "NurVerse Bot"
          git config user.email "actions@users.noreply.github.com"
          git add lib/screens/settings/settings_screen.dart
          git commit -m "Unify Settings Screen with Jamaat controls"
          git push
      - name: Remove one-time workflow
        run: |
          rm -f .github/workflows/unify-settings-screen.yml
          git add .github/workflows/unify-settings-screen.yml
          git commit -m "Remove one-time settings unification workflow"
          git push
