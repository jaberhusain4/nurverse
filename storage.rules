rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /dua_audio/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
        && request.resource.contentType == 'audio/mp4'
        && request.resource.size < 10 * 1024 * 1024;
    }
  }
}
